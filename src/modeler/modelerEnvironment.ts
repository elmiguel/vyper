// Studio viewport preview settings now live in the shared types layer (so the project design
// doc can persist them). Re-exported here for the modeler modules that import from this path.
export type { StudioTone, StudioEnv } from '@/types';
export { defaultStudioEnv } from '@/types';

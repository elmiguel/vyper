// ObjectGroups now lives in babylon/editmesh/islands (shared with the scene editor's Edit Mode);
// re-exported here so the Studio keeps importing it from this path until it's retired.
export { ObjectGroups } from '@/babylon/editmesh/islands';
